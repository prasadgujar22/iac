# =============================================================================
# listener.ora – Oracle Net Listener configuration
# Generated by Ansible for JMS HA POC (standalone DB node)
# =============================================================================

{{ listener_name }} =
  (DESCRIPTION_LIST =
    (DESCRIPTION =
      (ADDRESS = (PROTOCOL = TCP)(HOST = {{ ansible_host }})(PORT = {{ listener_port }}))
      (ADDRESS = (PROTOCOL = IPC)(KEY = EXTPROC{{ listener_port }}))
    )
  )

ADR_BASE_LISTENER = {{ oracle_base }}
