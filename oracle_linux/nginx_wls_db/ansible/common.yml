---
# =============================================================================
# common.yml – OS prerequisites for all nodes
# =============================================================================

- name: Common OS configuration for all nodes
  hosts: all
  gather_facts: true
  become: true
  tags: [common]
  roles:
    - role: common
